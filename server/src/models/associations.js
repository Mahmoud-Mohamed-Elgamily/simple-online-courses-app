// import User from './User'
// import Category from './Category'
// import Course from './Course'

// User.belongsToMany(Course, { through: "myCourses" });
// Course.belongsToMany(User, { through: "student" });

// Course.belongsToMany(Category, { through: "categories" });
// Category.belongsToMany(Course, { through: "courses" });
